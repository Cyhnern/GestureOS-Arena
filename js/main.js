/**
 * main.js
 * Uygulamanın giriş noktası. index.html bunu tek <script type="module"> olarak
 * yükler; geri kalan tüm modüller buradan (veya birbirinden) import edilir.
 */

import { initUI } from './ui.js';

initUI();
